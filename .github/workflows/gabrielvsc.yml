name: Meu Workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    steps:
      - name: Checkout do código
        uses: actions/checkout@v2

      # Outros passos para executar os testes

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v3
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
